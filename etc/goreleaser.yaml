---

version: 2

project_name: commit

dist: .build/dist

before:
  hooks:
    - go mod download

builds:
  - id: commit
    main: .
    binary: commit
    env:
      - CGO_ENABLED=0
      - SOURCE_DATE_EPOCH=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    mod_timestamp: '{{ .CommitTimestamp }}'
    flags:
      - -v
      - -trimpath
      - -buildvcs=false
    ldflags:
      - -s -w
      - -X github.com/hasansino/commit/internal/version.xBuildVersion={{ .Version }}

archives:
  - id: commit
    name_template: >-
      {{ .ProjectName }}_ {{- .Version }}_ {{- if eq .Os "darwin" }}macOS{{ else }}{{ .Os }}{{ end }}_ {{- if eq .Arch "amd64"
      }}x86_64 {{- else if eq .Arch "386" }}i386 {{- else }}{{ .Arch }}{{ end }}
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - LICENSE
      - README.md

homebrew_casks:
  - name: commit
    ids:
      - commit
    binaries:
      - commit
    homepage: https://github.com/hasansino/commit
    description: Commit helper tool
    skip_upload: auto
    repository:
      owner: hasansino
      name: homebrew-commit
      token: '{{ .Env.HOMEBREW_GITHUB_TOKEN }}'
    hooks:
      post:
        # Release checksums are signed with Sigstore; macOS binaries are not Apple-notarized.
        # Limit quarantine removal to the installed executable.
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/commit"]
          end

nfpms:
  - id: commit-packages
    package_name: commit
    vendor: hasansino
    homepage: https://github.com/hasansino/commit
    maintainer: hasansino <hasansino@users.noreply.github.com>
    description: Commit helper tool
    license: MIT
    formats:
      - deb
      - rpm
      - apk
    dependencies:
      - git
    recommends:
      - golang
    section: utils
    priority: optional
    file_name_template: >-
      {{ .PackageName }}_ {{- .Version }}_ {{- if eq .Os "darwin" }}macOS{{ else }}{{ .Os }}{{ end }}_ {{- if eq .Arch "amd64"
      }}x86_64 {{- else if eq .Arch "386" }}i386 {{- else if eq .Arch "arm64" }}aarch64 {{- else }}{{ .Arch }}{{ end }}
    contents:
      - src: LICENSE
        dst: /usr/share/licenses/commit/LICENSE
      - src: README.md
        dst: /usr/share/doc/commit/README.md
    deb:
      lintian_overrides:
        - statically-linked-binary
        - changelog-file-missing-in-native-package
    rpm:
      group: Applications/System
      compression: xz
    apk:
      signature:
        key_file: ""

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

snapshot:
  version_template: "{{ .Version }}-next"

changelog:
  sort: asc
  # Generate notes from the validated release commit's Git history.
  use: git
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - '^chore:'
      - Merge pull request
      - Merge branch
  groups:
    - title: 'Changes'
      order: 999

release:
  github:
    owner: hasansino
    name: commit
  target_commitish: "{{ .Commit }}"
  prerelease: auto
  make_latest: '{{ if .Prerelease }}false{{ else }}true{{ end }}'
  name_template: "{{.ProjectName}} v{{.Version}}"

# generate SBOMs for archives
sboms:
  - artifacts: archive

# sign checksums.txt
signs:
  - cmd: cosign
    signature: "${artifact}.sigstore.json"
    args:
      - "sign-blob"
      - "--bundle=${signature}"
      - "${artifact}"
      - "--yes"
    artifacts: checksum
    output: true
